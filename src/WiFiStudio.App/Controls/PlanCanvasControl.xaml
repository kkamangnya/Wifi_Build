<UserControl
    x:Class="WiFiStudio.App.Controls.PlanCanvasControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer
            x:Name="Scroller"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto"
            ZoomMode="Disabled">
            <Canvas
                x:Name="DrawingCanvas"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                PointerMoved="DrawingCanvas_OnPointerMoved"
                PointerPressed="DrawingCanvas_OnPointerPressed"
                PointerReleased="DrawingCanvas_OnPointerReleased"
                PointerWheelChanged="DrawingCanvas_OnPointerWheelChanged" />
        </ScrollViewer>

        <Border
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Margin="12"
            Padding="10"
            CornerRadius="6"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <StackPanel Spacing="6" Width="132">
                <TextBlock Text="RSSI" FontWeight="SemiBold" />
                <Grid Height="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="#E74C3C" />
                    <Border Grid.Column="1" Background="#F2C94C" />
                    <Border Grid.Column="2" Background="#27C93F" />
                </Grid>
                <Grid>
                    <TextBlock Text="-90" FontSize="11" HorizontalAlignment="Left" />
                    <TextBlock Text="-67" FontSize="11" HorizontalAlignment="Center" />
                    <TextBlock Text="-55" FontSize="11" HorizontalAlignment="Right" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
